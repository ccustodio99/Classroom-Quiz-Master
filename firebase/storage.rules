rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /orgs/{orgId}/classes/{classId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.token.org == orgId;
    }
  }
}
